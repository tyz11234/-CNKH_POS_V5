name: Run8 Candidate Probe (frozen)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Exact 96/96 candidate materialization verification completed before Windows gate debugging."
