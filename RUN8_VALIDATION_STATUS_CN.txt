CNKH Hardware POS V5 — Run #8 Stabilization 最终 Windows 验证状态

版本：5.0.0-alpha.4
日期：2026-08-11（Asia/Kuala_Lumpur）
当前分支：ci/run8-githubready-20260810
目标：Windows Installation Candidate

一、当前已完成并有真实 Windows runner 证据的项目

PASS — Ruff lint。
PASS — pytest / unittest / compileall / Source Self-Test。
PASS — 完整业务生命周期自动模拟：管理员/员工、客户、供应商、商品新增/编辑/删除、自动/手动 Barcode、进货与付款、库存变化、Staff 销售、% / RM Discount、结账进位、挂单/取单、退货、销售记录删除、盘点、报表、Excel、条码标签、80mm 小票、备份/恢复。
PASS — Windows GUI Acceptance 100% / 125% / 150%。
PASS — Admin EXE / Staff EXE PyInstaller 构建。
PASS — packaged Admin / Staff self-test。
PASS — Setup.exe 构建与静默安装。
PASS — installed Admin / Staff self-test。
PASS — installed Admin / Staff normal-launch smoke，必须观察到真实登录窗口。
PASS — release manifest / release package / Windows Artifact 上传链（Run #92）。

二、本阶段新增并已验证的功能

1. 商品条码标签打印：商品页新增 Barcode Labels；支持常见热敏标签尺寸，默认 50×30 mm；打印张数可自定义 1–999；支持 Windows 打印对话框与 PDF 证据输出。
2. 新增商品 Barcode：可明确选择“系统自动生成 EAN-13”或“手动输入”，继续使用原 CatalogService 唯一性校验。
3. 销售记录删除：按现有数据架构安全处理，不允许用破坏库存/报表一致性的裸 SQL 删除。
4. 结账进位：Cash/Card/QR 使用用户指定规则，例如 RM0.67→RM0.70、RM0.42→RM0.40、RM0.45 保持；原始 subtotal/discount 与 rounding 调整分开记录；Credit 欠账不做现金进位。
5. Discount：购物车与结账支持 % 或固定 RM 金额，并真实进入应付、销售明细、小票、退货与报表。
6. 80mm 小票：修复金额右侧裁切；后续人工证据又发现 Qt QTextDocument 打印路径出现黑块/乱码，已改为“先生成已验证 ReportLab 80mm PDF，再由 QtPdf 在 203dpi 热敏分辨率栅格化送入 QPrinter”。Focused Windows Run #3 已通过 Ruff、打印回归和实际 Qt 输出可读性门禁；新 Qt 小票人工确认中文、商品行及 RM 金额完整可读，有效内容横向覆盖率 0.7925。

三、标签与打印规格

- 商品条码标签默认：50×30 mm。
- 其他常见标签尺寸仍保留可选。
- 标签张数：1–999 可自定义。
- 80mm 小票：Windows 默认打印机或指定打印机；自动环境可生成 PDF 证据。
- 实体热敏打印机硬件仍只能在真实门店设备上做最终物理出纸确认；云端 Windows runner 可以验证 Windows 打印路径、页面尺寸、PDF/Qt 渲染与 EXE 打包。

四、最终发布硬门槛

同一个最终 commit 必须全部通过：Ruff、完整业务测试、unittest、compileall、Source Self-Test、100/125/150% GUI、Admin/Staff EXE、packaged self-tests、Setup.exe、silent install、installed self-tests、installed normal-launch、manifest/package、Artifact；并人工检查最终 GUI/打印证据。

Run #92 曾整条 Windows gate 绿色，但人工检查抓到 Qt 80mm 黑块，因此没有签为最终 Candidate。该真实缺陷已通过 focused Windows test 修复。随后条码标签默认规格调整为 50×30 mm，并同步所有相关测试。

当前状态：必须以本文件这次提交后触发的最新完整 Windows Release Gate 为最终依据。不得把较早 Run 的绿色结果拼接成最终通过。

五、安全与数据规则

- 不索取密码、Token 或其他凭证。
- 不删除或覆盖正式数据库/用户资料。
- migration 必须可回滚，升级前按规则备份。
- 金额使用整数 sen；数量使用 Decimal。
- Staff 不得获得 Admin 专属权限。
- 最终源码包不得包含真实数据库、用户数据、缓存、旧 EXE/Setup/Windows Artifact。
- 当前 GitHub 操作已由用户在本对话明确授权本项目最大权限。

当前结论：最终 Windows Release Gate 重跑中；在最新 commit 全链成功并人工证据复核完成之前，不宣称最终完成。